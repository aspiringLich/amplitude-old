
pub struct RawTrackConfig {
    pub title: String,
    pub description: String,
}

pub struct TrackConfig {
    pub id: String,
    pub title: String,
    pub description: String,
}

impl Item for TrackConfig {

}